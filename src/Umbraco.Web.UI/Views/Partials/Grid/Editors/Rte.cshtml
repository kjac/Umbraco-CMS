@model dynamic
@using Umbraco.Web.Templates
@{
    var value = TemplateUtilities.ParseInternalLinks(Model.value.ToString());
    value = TemplateUtilities.ResolveMediaFromTextString(value);
}

@Html.Raw(value)
